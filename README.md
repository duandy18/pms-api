# pms-api

Standalone PMS API service.

Current goal:
- PMS API becomes an independent backend process.
- PMS database migration will be handled later.
- WMS / OMS / Procurement / Finance will consume PMS through explicit API contracts.
